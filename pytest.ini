[pytest]
addopts = -v
testpaths = tests
# Base URL for E2E tests (override with --base-url for different port)
base_url = http://127.0.0.1:8765
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
